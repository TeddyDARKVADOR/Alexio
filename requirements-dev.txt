# Alexio — outillage de développement.
#
#   pip install -r requirements-dev.txt
#
# La suite de tests tourne sans micro, sans écran et sans clé API : voir
# tests/fake_ui.py et tests/conftest.py.

pytest>=8.0
pytest-asyncio>=0.24

# Couverture, pour tools/jarvis_health. La suite tourne sans, mais le
# tableau de bord affiche alors « non mesuré » plutôt que de deviner.
coverage[toml]>=7.6
pytest-cov>=5.0
pyflakes>=3.2
