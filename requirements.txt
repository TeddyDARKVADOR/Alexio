# Alexio — dépendances d'exécution.
#
# Ce fichier liste les intentions ; requirements.lock fige les versions réellement
# installées. Installer depuis le lock pour reproduire un environnement, depuis ce
# fichier pour en construire un neuf.
#
#   python -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#
# Python 3.11 ou 3.12 obligatoire : main.py utilise asyncio.TaskGroup et
# BaseExceptionGroup (3.11+), et plusieurs roues ci-dessous n'existent pas encore
# pour le 3.14 que livre Fedora 43.

# ── Interface ────────────────────────────────────────────────────────────────
PyQt6

# ── Modèle ───────────────────────────────────────────────────────────────────
# google-generativeai (SDK historique) a été retiré : aucun module ne l'importe.
google-genai>=2.8.0

# ── Audio ────────────────────────────────────────────────────────────────────
sounddevice
numpy

# ── Vision ───────────────────────────────────────────────────────────────────
pillow
opencv-python
mss

# ── Web ──────────────────────────────────────────────────────────────────────
requests
beautifulsoup4
ddgs>=9.0.0
playwright

# ── Bureau : portails, D-Bus ─────────────────────────────────────────────────
# D-Bus en Python pur, pour xdg-desktop-portal (capture d'écran, corbeille,
# fond d'écran) sous Wayland. Pas de compilation, pas de PyGObject — voir R-10.
jeepney; sys_platform == "linux"

# ── Contrôle du poste ────────────────────────────────────────────────────────
pyautogui
pyperclip
pygetwindow
psutil
send2trash

# ── Fichiers ─────────────────────────────────────────────────────────────────
python-pptx
youtube-transcript-api

# ── Dashboard distant ────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
cryptography
python-multipart
qrcode[pil]

# ── Windows uniquement ───────────────────────────────────────────────────────
comtypes;    sys_platform == "win32"
pycaw;       sys_platform == "win32"
win10toast;  sys_platform == "win32"
pywinauto;   sys_platform == "win32"
pywin32;     sys_platform == "win32"
